-- Compiled with roblox-ts v1.0.0-beta.10
local REMOTE_EVENTS = { "ServeEvent", "BlockEvent", "SpikeEvent" }
local REMOTE_FUNCTIONS = {}
return {
	REMOTE_EVENTS = REMOTE_EVENTS,
	REMOTE_FUNCTIONS = REMOTE_FUNCTIONS,
}
